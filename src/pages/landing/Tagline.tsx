import React from 'react';
import { Alert } from 'react-bootstrap';

const Tagline = () => {
    return (
        <Alert className="bg-primary text-white border-0 mb-0 rounded-0 text-center fs-15">
            <strong>Shreyu React v1.0 is here with fresh and super clean look and feel!</strong>
        </Alert>
    );
};

export default Tagline;
